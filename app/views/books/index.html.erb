<h1>Bookers</h1>

<table>

  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th></th>
    </tr>
  </thead>

  <tbody>

    <tr>
      <td>リーダブルコード</td>
      <td>より良いコードを書くためのシンプルで実践的なテクニック</td>
      <th>
        <a></a>
      </th>
      <th>
        <a></a>
      </th>
      <th>
        <a></a>
      </th>

    </tr>



  </tbody>

  <tfoot>
    <tr>
      <td colspan="3">合計</td>
    </tr>
  </tfoot>
</table>

<%  @books.each do |book| %>

  <p>タイトル</p>

    <%= book.title %>
    <%= book.body %>
    <%= link_to 'show', book_path(book.id) %>
    <%= link_to 'edit', edit_book_path(book.id) %>
    <%= link_to 'destroy', book_path(book.id),method: :delete,"data-confirm" => "Are You Sure？"  %>
<% end %>



<h1>New book</h1>
<% if @new_book.errors.any? %>
    <%= @new_book.errors.count %>An error occurred because it was blank
    <ul>
      <% @new_book.errors .full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
<% end %>
<!-- form_with部分 -->
<%= form_with model: @new_book do |f| %>
  <h4>本のタイトル</h4>
  <%= f.text_field :title %>

  <h4>感想</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>