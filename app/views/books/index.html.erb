

<%  @lists.each do |book| %>

  <p>タイトル</p>
  <span><%= list.title %></span>

<% end %>

<h1>New book</h1>
<!-- form_with部分 -->
<%= form_with model: @list do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :Title %>

  <h4>本文</h4>
  <%= f.text_area :Body %>

  <%= f.submit 'Create Book' %>
<% end %>